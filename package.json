{
    "name": "bitran",
    "version": "1.0.0",
    "type": "module",
    "main": "dist/index.js",
    "scripts": {
        "prepare": "yarn clear && yarn build",
        "build": "yarn clear && npx tsc",
        "clear": "npx rimraf dist"
    },
    "devDependencies": {
        "rimraf": "^5.0.1",
        "typescript": "^5.1.6"
    },
    "dependencies": {
        "yaml": "^2.3.1"
    }
}
